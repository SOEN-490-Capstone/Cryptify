import { IconDefinition, IconName, IconPrefix } from "@fortawesome/fontawesome-svg-core";

export const faEthereum: IconDefinition = {
    icon: [
        // SVG viewbox width (in pixels)
        320,
        // SVG viewbox height (in pixels)
        512,
        // Aliases (not needed)
        [],
        // Unicode as hex value (not needed)
        "f42e",
        // SVG path data
        "M311.9 260.8L160 353.6 8 260.8 160 0l151.9 260.8zM160 383.4L8 290.6 160 512l152-221.4-152 92.8z",
    ],
    iconName: "fac-ethereum" as IconName,
    prefix: "fac-brands" as IconPrefix,
};
