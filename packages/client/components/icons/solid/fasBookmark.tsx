import { IconDefinition, IconName, IconPrefix } from "@fortawesome/fontawesome-svg-core";

export const fasBookmark: IconDefinition = {
    icon: [
        // SVG viewbox width (in pixels)
        384,
        // SVG viewbox height (in pixels)
        512,
        // Aliases (not needed)
        [],
        // Unicode as hex value (not needed)
        "",
        // SVG path data
        "M384 48V512l-192-112L0 512V48C0 21.5 21.5 0 48 0h288C362.5 0 384 21.5 384 48z",
    ],
    iconName: "fas-bookmark" as IconName,
    prefix: "fas-regular" as IconPrefix,
};
